<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/red_err"
    tools:context=".BannedActivity">

    <ImageView
        android:id="@+id/img_err"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:src="@drawable/baseline_report_gmailerrorred_24"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/banned_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textColor="@color/yellowStar"
        android:textSize="30dp"
        android:text="@string/ban_text"
        app:layout_constraintTop_toBottomOf="@id/img_err" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/logout_banned"
        style="@style/smallButtonStyle"
        android:layout_width="320dp"
        android:layout_height="wrap_content"
        android:text="@string/btn_logout"
        app:cornerRadius="50dp"
        android:background="@color/yellowStar"
        android:textColor="@color/red_err"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>


</androidx.constraintlayout.widget.ConstraintLayout>